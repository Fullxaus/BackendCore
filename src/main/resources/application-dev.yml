# Profile: dev — PostgreSQL (Docker)
# Схема БД управляется только Liquibase (db/changelog). schema.sql не выполняется.
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/crm_db
    username: postgres
    password: C1m5r8d9h16!
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: never
  liquibase:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
