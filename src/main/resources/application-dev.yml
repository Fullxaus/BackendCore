# Profile: dev — PostgreSQL (Docker)
# Схема БД управляется Liquibase (db/changelog). schema.sql не выполняется.
# ddl-auto: update — резерв: Hibernate создаёт недостающие таблицы (например deal_product),
# если Liquibase ещё не применил миграции к этой БД.
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/crm_db
    username: postgres
    password: C1m5r8d9h16!
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: never
  liquibase:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
